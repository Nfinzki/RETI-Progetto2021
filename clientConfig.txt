# Registry port for RMI
REGISTRY-PORT=7777

# Host where the registry is
REGISTRY-HOST=localhost

# Service name for RMI
RMI-REGISTER=REGISTER-SERVICE

# Server IP address
SERVER-IP=localhost

# Server port
TCP-PORT=6666

# Size of the buffer
BUFFER-SIZE=16384

# Callback service name
RMI-CALLBACK=RMI-FOLLOWER-CALLBACK